spring.application.name=BootMSProj06-ConfigServer

#Config server port
server.port=8888

#Link with Native config file
spring.cloud.config.server.native.search-locations=classpath:/nconfig

# activate native profile of parent project to work with native config file
spring.profiles.active=native
